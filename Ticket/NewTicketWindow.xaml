<Window x:Class="TicketingApp.NewTicketWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Nuovo Ticket" Height="300" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Mittente:" Margin="0,0,5,5"/>
        <TextBox x:Name="EmailTextBox" Grid.Row="0" Grid.Column="1" Margin="0,0,0,5"/>

        <Label Grid.Row="1" Grid.Column="0" Content="Oggetto:" Margin="0,0,5,5"/>
        <TextBox x:Name="SubjectTextBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,5"/>

        <Label Grid.Row="2" Grid.Column="0" Content="Corpo:" Margin="0,0,5,5"/>
        <TextBox x:Name="BodyTextBox" Grid.Row="2" Grid.Column="1" AcceptsReturn="True" TextWrapping="Wrap"/>

        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Crea" Width="80" Margin="5,0" Click="CreateButton_Click"/>
            <Button Content="Annulla" Width="80" Margin="5,0" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>